// Package rask carries module-level metadata. The CLI and library live in
// cmd/rask and pkg/cluster; this file exists so tagpr can manage the
// version.
package rask

// Version is the current version of rask.
const Version = "0.1.0"
